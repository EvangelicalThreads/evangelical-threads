// Free shipping unlocks past this subtotal — the only place this number
// lives. Shared by the cart's shipping-progress bar (Navbar.tsx) and the
// PDP's Shipping & Returns drawer (ProductDetailsClient.tsx) so the two
// never drift apart.
export const FREE_SHIPPING_THRESHOLD = 50;
